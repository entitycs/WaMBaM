---
description: Applied to Love2D state machine files to maintain clear separation of concerns
alwaysApply: true
---

Separate state machine logic from menu-specific code. The state machine should handle transitions and core logic, while menu-specific features (shaders, positioning, joystick mappings) should be modularized into separate components.